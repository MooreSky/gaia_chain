import { generateAddress } from "../../../app/crypto/address"
import { H160 } from "../../../bridge/rust/pi_math/hash"

export default (describe: any, it: any, chai: any) => {

    describe("app/crypto/address", () => {
        describe("generateAddress", () => {
            it("1", () => {
                
            });
        });
    });
}